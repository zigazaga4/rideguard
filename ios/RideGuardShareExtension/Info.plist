<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDisplayName</key>
	<string>RideGuard</string>
	<key>CFBundleName</key>
	<string>RideGuardShare</string>
	<key>CFBundlePackageType</key>
	<string>XPC!</string>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.share-services</string>
		<!-- A plain UIViewController, not a storyboard and not
		     SLComposeServiceViewController: the sheet shows a verdict, it does
		     not compose a post, so the stock compose UI is the wrong shape. -->
		<key>NSExtensionPrincipalClass</key>
		<string>$(PRODUCT_MODULE_NAME).ShareViewController</string>
		<key>NSExtensionAttributes</key>
		<dict>
			<!-- Exactly one image. Text-only shares are rejected on purpose:
			     without a screenshot there is nothing to OCR, and offering the
			     extension for every share target would be noise in the sheet. -->
			<key>NSExtensionActivationRule</key>
			<dict>
				<key>NSExtensionActivationSupportsImageWithMaxCount</key>
				<integer>1</integer>
				<key>NSExtensionActivationSupportsText</key>
				<false/>
				<key>NSExtensionActivationSupportsWebURLWithMaxCount</key>
				<integer>0</integer>
				<key>NSExtensionActivationSupportsFileWithMaxCount</key>
				<integer>0</integer>
			</dict>
		</dict>
	</dict>
</dict>
</plist>
